APP_NAME=Spendly
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stderr
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration
# Option 1: SQLite (simple, single-container deployment)
DB_CONNECTION=sqlite
DB_DATABASE=database/database.sqlite

# Option 2: PostgreSQL (required for ML features)
# DB_CONNECTION=pgsql
# DB_HOST=postgres
# DB_PORT=5432
# DB_DATABASE=spendly
# DB_USERNAME=spendly
# DB_PASSWORD=spendly

# Option 3: MySQL/MariaDB
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=spendly
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"

# GOCARDLESS_SECRET_ID=
# GOCARDLESS_SECRET_KEY=
# GOCARDLESS_USE_MOCK=true
# GOCARDLESS_MOCK_DATA_PATH=sample_data/gocardless_bank_account_data

OCTANE_SERVER=frankenphp
COMPOSE_RUN_RM=1

# FrankenPHP / Container memory management
# Set GOMEMLIMIT to ~90% of container memory limit to prevent OOM
# GOMEMLIMIT=450MiB

# ML Service Configuration
# Set ML_ENABLED=true when using PostgreSQL + ML stack (compose.ml.yml)
ML_ENABLED=false
ML_SERVICE_URL=http://ml-service:8001
# Shared secret; must match the ML service's ML_SERVICE_TOKEN. Required when ML_ENABLED=true.
ML_SERVICE_TOKEN=

# Queue Configuration
# Use 'redis' when ML is enabled, 'database' for simple SQLite setup
QUEUE_CONNECTION=database
# QUEUE_CONNECTION=redis
REDIS_URL=redis://redis:6379

# Transfer Detection (optional tunables, defaults shown — see config/transfers.php)
# TRANSFERS_DATE_GAP_DAYS=3
# TRANSFERS_AMOUNT_TOLERANCE=0.01
# TRANSFERS_AMOUNT_TOLERANCE_PERCENT=0.0
# TRANSFERS_HEURISTIC_AUTO_THRESHOLD=0.60
# TRANSFERS_HEURISTIC_REVIEW_THRESHOLD=0.35
# TRANSFERS_CROSS_CURRENCY_ENABLED=true
# TRANSFERS_CROSS_CURRENCY_TOLERANCE_PERCENT=3.0
# TRANSFERS_CROSS_CURRENCY_AUTO_MARK=false
# TRANSFERS_SINGLE_LEG_AUTO_MARK=true
# TRANSFERS_WINDOW_PADDING_DAYS=3

# Recurring Detection (optional tunables, defaults shown — see config/recurring.php)
# RECURRING_LOOKBACK_MONTHS_DEFAULT=24
# RECURRING_MIN_CONFIDENCE=40
