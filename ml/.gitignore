venv/
.env
__pycache__/
.pytest_cache/
*.pyc
.ipynb_checkpoints/

# ML data artifacts (persisted at runtime) — may derive from real user data,
# never commit anything under data/ except the .gitkeep placeholders.
data/vectors/*
data/models/*
!data/vectors/.gitkeep
!data/models/.gitkeep
